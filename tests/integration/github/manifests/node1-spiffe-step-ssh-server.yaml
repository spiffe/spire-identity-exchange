apiVersion: spire.spiffe.io/v1alpha1
kind: ClusterStaticEntry
metadata:
  name: node1-spiffe-step-ssh-server
spec:
  parentID: spiffe://${SPIFFE_TRUST_DOMAIN}/agent/node1
  spiffeID: spiffe://${SPIFFE_TRUST_DOMAIN}/spiffe-step-ssh-server
  selectors:
  - systemd:id:spiffe-step-ssh-server@main.service
