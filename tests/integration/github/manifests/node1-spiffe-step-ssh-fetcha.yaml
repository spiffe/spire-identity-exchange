apiVersion: spire.spiffe.io/v1alpha1
kind: ClusterStaticEntry
metadata:
  name: node1-spiffe-step-ssh-fetchca
spec:
  parentID: spiffe://${SPIFFE_TRUST_DOMAIN}/agent/node1
  spiffeID: spiffe://${SPIFFE_TRUST_DOMAIN}/spiffe-step-ssh-fetchca
  dnsNames:
  - "spiffe-step-ssh-fetchca.${SPIFFE_TRUST_DOMAIN}"
  selectors:
  - systemd:id:spiffe-step-ssh-fetchca@main.service
